from backend.app.db.session import SessionLocal; from backend.agents.orchestrator import AgentOrchestrator; from backend.app.db.models import User; db = SessionLocal(); user = db.query(User).filter_by(id=1).first(); o = AgentOrchestrator(db, user); print(o.run_query('What does our internal strategy playbook highlight as the primary Optimization Levers for External Alumina?', {})); db.close()
